import 'package:flutter/material.dart';

class ExamInfoScreen extends StatelessWidget {
  const ExamInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('考试信息'),
      ),
      body: const Center(
        child: Text('考试信息页面 - 开发中'),
      ),
    );
  }
}
